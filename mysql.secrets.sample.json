{
  "_comment": "Copy this file to mysql.secrets.json (gitignored) and adjust. It supplies the connection the INTEGRATION TESTS use. Those tests drop every table on each run, so point them at a throwaway database - never at one holding data you care about. The defaults below match docker-compose.yml.",
  "ConnectionStrings": {
    "AstTest": "Server=localhost;Port=3306;Database=ast_test;User ID=ast;Password=ast-dev-only;AllowUserVariables=true;UseAffectedRows=false"
  }
}
